rules_version = '2';

// LOCAL DEVELOPMENT RULES — fully open so the emulator never blocks you.
// Do NOT deploy these to production. Tighten before `firebase deploy`.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
